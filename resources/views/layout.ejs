<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Realtime Pizza App</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/public/style/app.css" />
    <link
      rel="stylesheet"
      href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css"
    />
    <!-- <link rel="shortcut icon" type="image/png" href="./favicon.png" /> -->
  </head>
  <body>
    <nav class="container mx-auto flex items-center justify-between py-4">
      <a href="/">
        <div>
          <img src="/public/img/logo.png" alt="logo" />
        </div>
      </a>
      <div>
        <ul class="flex items-center">
          <li class="ml-6"><a href="#">Menu</a></li>
          <% if (user) { %> <% if (user.role === 'admin') { %>
          <li class="ml-6"><a href="/admin/orders">Orders</a></li>
          <% } else { %>
          <li class="ml-6"><a href="/customer/orders">Orders</a></li>
          <% } %>
          <li class="ml-6">
            <form id="logout" action="/logout" method="POST">
              <a onclick="document.getElementById('logout').submit()" href="#"
                >Logout</a
              >
            </form>
          </li>
          <% } else { %>
          <li class="ml-6"><a href="/register">Register</a></li>
          <li class="ml-6"><a href="/login">Login</a></li>
          <% } %>

          <li class="ml-6">
            <a
              href="/cart"
              class="inline-block px-3 py-2 rounded-full flex items-center"
            >
              <span class="cart-counter font-bold text-white px-0 lg:pr-3"
                ><%=session.cart ? session.cart.totalQty :'' %></span
              >
              <img class="cart" src="/public/img/cart.png" alt="cart-logo"
            /></a>
          </li>
        </ul>
      </div>
    </nav>
    <%-body%>
    <script src="/public/js/app.js"></script>
    <script src="/socket.io/socket.io.js"></script>
  </body>
</html>
